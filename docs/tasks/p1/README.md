# Phase 1 Tasks

Phase 1 implementation prompts will be generated here after:

`/prompt-ass -> /prompt-plan -> /prompt-write p1`

Validate the generated stack with:

`npm run codex:phase:validate -- p1`

Execute with:

`npm run codex:phase -- p1`

All Phase 1 prompts are runner-eligible and must contain:

`- Browser required: no.`

Current Phase 1 authority:
- `BOOT.md`;
- `docs/project-overview.md`;
- `docs/architecture.md`;
- `docs/workflow.md`;
- `docs/stability-contract.md`;
- `docs/planning/p1-local-agent-foundation/decision-record.md`;
- `docs/roadmap/mvp-roadmap.md`.

Phase 1 proves one bounded streamed model turn plus read-only tool infrastructure. The autonomous multi-turn tool loop and write/process capabilities belong to Phase 2.
