TASK: Correction 9 / P3 — Text-file edit fidelity correction closeout

MODEL / REASONING / USAGE
- Recommended configuration: `Sol Medium`.
- Browser required: no.

VERSIONING
- Required unchanged project version: `0.9.10`.

GOAL

Perform an evidence-only closeout of `c9-text-file-edit-fidelity` from the exact P2 candidate.

Read:
- `BOOT.md`;
- current architecture/workflow/stability/roadmap contracts;
- current Phase 9 authority and closeout;
- prior c9 correction closeouts;
- `docs/tasks/c9-text-file-edit-fidelity/{prompt-assessment,implementation-plan,P2-live-evidence}.md`;
- exact current source/tests.

Audit separately:

TEXT FRAMING EVIDENCE
- full-file framing classifier;
- LF/CRLF/mixed/none semantics;
- final-newline state;
- full-file evidence despite bounded/truncated read text;
- invalid UTF-8/NUL omission behavior;
- same-open-file SHA/framing behavior;
- structured inspection propagation.

MUTATION FIDELITY GUARD
- `write_file` remains verbatim full replacement;
- `apply_patch` preserves untouched bytes;
- default existing-text framing preservation;
- bounded recoverable rejection;
- unchanged file/SHA after rejection;
- explicit intentional framing-change acknowledgement;
- new-file/non-text compatibility;
- stale-SHA behavior unchanged;
- provider guidance favors patching for localized edits.

LIVE GATE A
- read framing evidence;
- first mutation request;
- framing rejection/retry if exercised;
- final mutation byte count/SHA/framing;
- exact 60-byte file;
- George-owned V1;
- completed TaskState;
- hidden acceptance;
- calls/rounds/tokens/context/budgets/timing/intervention.

LARGER LIVE GATES
- Gate B only if Gate A permitted it;
- Gate C only if Gate B permitted it;
- stack/task/validation/hidden-acceptance truth;
- any framing-related regressions.

INHERITED QUALIFIED BOUNDARIES
- production task-stack execution;
- structured convergence/evidence/budget/direct-validation controls;
- SHA mutation preconditions;
- mandatory INSPECT tool execution;
- INSPECT successful-tool-round completion;
- exception-safe live trace retention;
- Bubblewrap containment;
- unchanged context/runtime policy;
- native TUI Evidence Gap unless independently resolved.

Create:

`docs/tasks/c9-text-file-edit-fidelity/closeout.md`

Report separately:
- Implementation Complete: yes/no;
- Text Framing Evidence Qualified: yes/no;
- Mutation Fidelity Guard Qualified: yes/no;
- Gate A Qwen Work-Unit Convergence Qualified: yes/no;
- Gate B Greenfield Qualified / Not Run;
- Gate C Existing-App Qualified / Not Run;
- Overall c9-text-file-edit-fidelity Qualified: yes/no.

QUALIFICATION RULES

Text Framing Evidence Qualified requires deterministic full-file framing evidence and structured propagation regressions Green.

Mutation Fidelity Guard Qualified requires deterministic rejection/preservation/explicit-opt-in coverage Green and no silent normalization.

Qwen Work-Unit Convergence Qualified requires Gate A Green:
- exact required bytes including final newline;
- V1 pass;
- completed/verified TaskState;
- hidden acceptance Green;
- zero intervention;
- no exhaustion;
- <=10 model tool calls;
- <=10 provider rounds.

Overall correction qualification requires Gate A Green.

Do not waive Gate A.

Do not:
- repair implementation;
- rerun live workloads;
- rewrite historical failures;
- owner-close Phase 9;
- open Phase 10;
- change BOOT/roadmap;
- change package version.

Required proof:
- focused P1 regressions;
- frozen deterministic replay;
- Phase 9 integration;
- inherited c9 affected floor;
- `npm run typecheck`;
- `npm run test:runner`;
- appropriate broad characterization with unrelated OpenTUI evidence retained truthfully;
- `git diff --check`;
- package exactly `0.9.10`;
- no root `package-lock.json`.

Owner acceptance and Phase 9 owner closeout remain separate actions.
