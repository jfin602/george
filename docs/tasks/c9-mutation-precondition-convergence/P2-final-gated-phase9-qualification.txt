TASK: Correction 9 / P2 — Final mutation-convergence Phase 9 live qualification

MODEL / REASONING / USAGE
- Recommended configuration: `Sol Medium`.
- Browser required: no.

VERSIONING
- Required unchanged project version: `0.9.10`.

GOAL

Qualify the exact P1 production candidate with deterministic pre-gates and one fresh gated live sequence:

Gate A -> Gate B2 -> Gate C2.

This prompt is evidence/qualification only.

Read:
- `BOOT.md`;
- `AGENTS.md`;
- current Phase 9 qualification authority;
- `docs/planning/c9-mutation-precondition-convergence/{decision-record,qualification-plan}.md`;
- `docs/tasks/c9-mutation-precondition-convergence/{prompt-assessment,implementation-plan}.md`;
- `docs/tasks/c9-mutation-intent-authority/{P2-live-evidence,closeout}.md`;
- `docs/tasks/c9-greenfield-instrument-alignment/{P2-live-evidence,closeout}.md`;
- exact P1 code/tests;
- frozen Gate A fixture/acceptance/helper;
- unchanged v2 fixtures/metadata/acceptance;
- repository-owned live-work artifact writer.

Do not modify production code or live-work fixture/task semantics in P2.

HISTORICAL EVIDENCE

Preserve without rewriting:
- prior Gate A Green from `c9-mutation-intent-authority`;
- historical v1 Not Green attempts;
- first official `greenfield-express-v2` B2 Not Green attempt.

The old Gate A remains historical Green, but P1 changed production agent-loop/tool/recovery behavior, so it must be freshly rerun for current qualification.

DETERMINISTIC PRE-GATE

Require Green before live model work:

- exact missing-SHA convergence regression;
- exact missing-parent/pre-dispatch failure regression;
- `create_directory` path/safety/idempotence tests;
- Standard/Workspace Autonomous permission tests;
- directory interruption/recovery tests;
- same-response terminal-tool-evidence regression;
- structured implementation/correction tool-surface test;
- existing SHA/framing/mutation-intent authority tests;
- frozen Gate A deterministic replay;
- production StackState/task-state/budget/recovery floors;
- Phase 9 integration;
- v1/v2 fixture/task/metadata/digest/acceptance immutability;
- `npm run typecheck`;
- `npm run test:runner`;
- `npm test` under supported Node 26;
- `git diff --check`.

Verify package `0.9.10` and no root `package-lock.json`.

If a required correction-specific deterministic gate is Not Green, stop before live work.

RUNTIME CONTROL

Use exactly:

`qwen3-coder-30b-a3b-instruct@q4_k_m`

Use supported Node 26.

Before live work:
- print `MANUAL CHECK: LM Studio GPU Offload must show 26`;
- capture exact Node/npm and REST-visible model/runtime controls;
- do not change context/runtime/convergence/retry/correction/stage/run-budget settings;
- unconfirmed UI-only GPU Offload 26 remains controlled-latency Evidence Gap, not a functional blocker.

GATE A — FRESH FROZEN EDIT REPLAY

Run exactly one official attempt against the unchanged frozen Gate A workload.

Use:
- repository-owned fair Phase 9 qualification approval helper;
- unchanged target fixture;
- unchanged validation;
- unchanged hidden acceptance;
- production structured task service.

Record:
- initial/final bytes SHA/framing;
- requested/started/terminal tools;
- approvals and mutation intent;
- V1;
- TaskState;
- hidden acceptance;
- provider attempts/logical rounds/retries;
- tokens/context/headroom;
- task/stage/correction/run budgets;
- timing;
- human intervention.

Gate A Green requires:
- exact required 60-byte edit;
- V1 Green;
- completed/verified TaskState;
- hidden acceptance Green;
- zero human coding intervention;
- no task/stage/correction/run-budget exhaustion;
- <=10 model-requested tool calls;
- <=10 logical provider rounds.

If Gate A is Not Green:
- STOP;
- do not run B2 or C2;
- do not repair or rerun.

GATE B2 — GREENFIELD-EXPRESS-V2

Only after fresh Gate A Green.

Run exactly one fresh official attempt.

Use unchanged:
- `greenfield-express-v2`;
- instrument version 2 and recorded taskStackSha256;
- behavioral acceptance version 1;
- exact deliberate prerequisite `npm install express@5.1.0`;
- production `StructuredTaskStackApplicationService`.

Do not edit the instrument or create v3.

Record:
- dependency authorization/command/result;
- task-by-task StackState;
- each top-level INSPECT;
- every provider attempt/round/retry;
- every model-requested/registered/started/terminal tool call;
- missing-SHA failures and explicit reads/retries;
- missing-parent failures;
- `create_directory` calls/results;
- successful mutations;
- declared George-owned validations;
- hidden acceptance;
- duplicate/no-progress evidence;
- context/profile/tokens/headroom;
- task/stage/correction/run budgets;
- timing;
- intervention count.

B2 Green requires ALL:
- P1/P2/P3 complete in strict order;
- all declared validations Green;
- final StackState completed;
- hidden acceptance Green;
- zero human coding intervention;
- no task/stage/correction/run-budget exhaustion;
- no fixture fallback/title heuristic/manual coding.

If B2 is Not Green:
- STOP;
- do not run C2;
- do not repair/rerun.

GATE C2 — EXISTING-EXPRESS-FEATURE-V2

Only after B2 Green.

Run exactly one official attempt using unchanged:
- `existing-express-feature-v2`;
- fixture version/source digest;
- task digest;
- acceptance version 1;
- dependency/network prerequisite;
- production `StructuredTaskApplicationService`.

Record:
- fixture commit;
- INSPECT;
- mutations/approvals;
- focused + broad validation;
- baseline preservation;
- TaskState;
- hidden acceptance;
- calls/rounds/tokens/context/budgets/timing/intervention.

C2 Green requires:
- focused validation Green;
- broad validation Green;
- completed/verified TaskState;
- baseline behavior preserved;
- hidden acceptance Green;
- zero intervention;
- no task/stage/correction/run-budget exhaustion.

COMMITTED SAFE EVIDENCE

Create:

`docs/tasks/c9-mutation-precondition-convergence/P2-live-evidence.md`

Also persist the repository-defined bounded safe artifact envelopes under tracked paths:

- `docs/tasks/c9-mutation-precondition-convergence/evidence/gate-a/attempt.json`
- `docs/tasks/c9-mutation-precondition-convergence/evidence/gate-a/trace.json`
- equivalent `gate-b2/` and `gate-c2/` only when reached.

Use `writeLiveWorkArtifacts()` directly with those directories, or copy exactly its safe bounded envelope/trace outputs without semantic alteration.

A sanitized `evidence/runtime.json` may record runtime provenance.

Do NOT commit:
- raw SSE/provider streams;
- assistant/tool raw bodies;
- write/patch content;
- secrets/environment;
- fixture workspaces;
- node_modules;
- unrestricted process output.

In `P2-live-evidence.md`, record:
- exact P1 candidate/package;
- deterministic pre-gate;
- runtime provenance;
- historical evidence preserved;
- each reached gate result;
- every committed evidence JSON path + SHA-256;
- Green / Not Green / Evidence Gap matrix.

Closeout truth must remain conservative.

Do not:
- modify production code;
- alter v1/v2 tasks/metadata/acceptance;
- retune limits/runtime/context;
- rerun a failed gate;
- owner-close Phase 9;
- open Phase 10.

Package stays exactly `0.9.10`; no root lockfile; `git diff --check`.
