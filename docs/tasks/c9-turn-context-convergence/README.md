# c9-turn-context-convergence

Production Phase 9 correction after the scoped qualification Gate B2 continuation failure.

The current official evidence is:
- qualification preflight policy: Green;
- fresh Gate A: Green;
- fresh `greenfield-express-v2` Gate B2: Not Green;
- Gate C2: Not Run.

B2 reached corrected mutation execution, then continuation context grew from a 1,319-token initial assembly to an estimated 28,306 tokens against the frozen ordinary 8,192 ceiling.

Source review found that successful mutation tool results carry internal Git working-tree snapshots into provider continuation. A separate ordinary transcript also showed provider-reported ordinary overshoot and globally injected coding-workflow guidance causing `hi` to expand into repository inspection.

This correction:
- separates canonical tool evidence from bounded provider-visible results;
- grounds continuation safety in provider usage when available;
- allows adaptive ordinary -> medium -> large continuation-envelope promotion without source reassembly;
- preserves fixed-profile and >large fail-closed behavior;
- restores ordinary TUI base-agent wiring;
- adds bounded greeting and synthetic three-file live smokes;
- reruns fresh Gate A -> B2 -> C2 only after deterministic and scoped preflight Green.

Prompt order:
1. P1 — canonical/provider tool-result projection;
2. P2 — provider-grounded continuation accounting + adaptive envelope promotion;
3. P3 — ordinary TUI base-agent wiring + live-smoke instrumentation;
4. P4 — scoped preflight + live smokes + fresh Gate A/B2/C2;
5. P5 — evidence-only closeout.

Package remains exactly `0.9.10`.

Authority:
- `docs/planning/c9-turn-context-convergence/decision-record.md`;
- `docs/planning/c9-turn-context-convergence/qualification-plan.md`;
- current Phase 9 decision/qualification authority;
- `docs/tasks/c9-qualification-preflight-scope/closeout.md`.

Correction broad-regression baseline:
`d7804684801fa8418050c34d9ef097ca1adf80b6`.

No profile retuning, v3 instruments, historical evidence rewrite, Phase 9 owner closeout, or Phase 10 opening belongs in this stack.
