TASK: Correction 9 / P6 — Structured convergence and task-stack correction closeout

MODEL / REASONING / USAGE
- Recommended configuration: `Sol Medium`.
- Browser required: no.

VERSIONING
- Required unchanged project version: `0.9.10`.

GOAL

Perform an evidence-only closeout of `c9-structured-task-convergence` after the correction expanded P4 to complete the missing Phase 9 production task-stack execution boundary.

Read:
- `BOOT.md`;
- current product/architecture/workflow/stability/roadmap contracts;
- Phase 9 authority;
- latest Phase 9 closeout/live evidence;
- all c9 prompt assessment/implementation plan/P1-P5 evidence;
- exact current source/tests.

Audit separately:

CONVERGENCE DIAGNOSIS
- inspection -> implementation evidence discontinuity;
- validation failure -> correction diagnostic discontinuity;
- structured nested budget reset;
- validation-only provider round;
- broad stage loop limits/repeated unchanged reads;
- live P4/greenfield/existing failures that exposed those defects.

CONVERGENCE IMPLEMENTATION
- bounded inspection evidence handoff;
- bounded validation failure diagnostics;
- one task-wide run budget;
- direct George-owned literal validation;
- structured-stage call/round ceilings;
- exact repeated replay-safe local-read/no-progress guard;
- ordinary chat/tool-loop non-regression.

PRODUCTION STACK GAP
- P1 finding that `validateTaskStack` lacked a production caller/executor;
- new canonical StackState/session persistence;
- stack validation before first execution;
- strict ordinal task progression;
- fail-stop after first non-completed task;
- preservation of per-task TaskState/history;
- safe durable reopen/resume;
- presentation-safe stack projection;
- live-work runner delegation to the production stack service rather than owning progression.

QUALIFICATION
- deterministic stack fail-stop test proving P1 failure produces zero P2/P3 provider calls;
- durable resume test proving completed P1 is not re-executed;
- hidden acceptance isolation;
- frozen Gate A live P4 result;
- Gate B greenfield production-stack result only if Gate A allowed it;
- Gate C existing-app result only if Gate B allowed it;
- call/round/token/time deltas versus preserved Phase 8 and first Phase 9;
- inherited Bubblewrap containment truth;
- inherited native-TUI Evidence Gap unless actually resolved;
- every remaining Not Green/Evidence Gap.

Create/update:
`docs/tasks/c9-structured-task-convergence/closeout.md`

The closeout must state separately:
- Implementation Complete: yes/no;
- Convergence Correction Qualified: yes/no;
- Production Stack Boundary Qualified: yes/no;
- Overall c9 Correction Qualified: yes/no.

QUALIFICATION RULES

Convergence Correction Qualified requires:
- deterministic correction tests Green;
- Gate A Green under exact functional + <=10 call + <=10 round criteria.

Production Stack Boundary Qualified requires:
- deterministic stack validation/fail-stop/resume Green;
- if Gate A permitted greenfield live execution, Gate B result recorded truthfully;
- a Gate B live failure makes Phase 9 usefulness Not Green but does not erase deterministic stack-boundary correctness.

Overall c9 Correction Qualified requires:
- convergence correction qualified;
- production stack deterministic boundary qualified;
- no unresolved implementation defect that invalidates Phase 9's claimed structured execution architecture.

Do not waive a Not Green Gate A.

Greenfield/existing-app remain additional Phase 9 end-to-end usefulness gates and must remain explicit even if deterministic stack correctness is Green.

Do not:
- repair implementation;
- rerun live workloads;
- modify convergence/stack limits;
- rewrite historical Phase 8/first Phase 9 evidence;
- owner-close Phase 9;
- advance/open Phase 10;
- change BOOT/roadmap phase state;
- change package version.

Required proof:
- `npm run typecheck`;
- `npm run test:runner`;
- focused c9 convergence + stack regression suite;
- `node --test test/integration/phase9-qualification.test.ts`;
- affected session/recovery/agent-loop tests;
- appropriate broader tests with any unrelated OpenTUI failures retained visibly;
- `git diff --check`;
- package exactly `0.9.10`;
- no root `package-lock.json`.

Owner acceptance and Phase 9 owner closeout remain separate actions after this correction.
