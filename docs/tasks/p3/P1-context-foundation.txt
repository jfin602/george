TASK: Phase 3 / P1 — Context source model, routing, and budget foundation

MODEL / REASONING / USAGE
- Recommended configuration: `Terra High`.
- Browser required: no.

VERSIONING
- This prompt's assigned project version is `0.3.1`.

GOAL

Build the provider-independent Phase-3 context foundation without replacing the live agent-loop context path yet.

Read `BOOT.md`, `AGENTS.md`, the Phase-3 decision record, `docs/tasks/p3/prompt-assessment.md`, and `docs/tasks/p3/implementation-plan.md`; inspect the current workspace/config/application tests before editing.

Introduce a typed context source/assembly layer with stable source identity, trust/source class, deterministic precedence/order, active-vs-routed role, estimated size, and explicit admitted/omitted/deferred/rejected diagnostics.

Support optional user-level George config sources using platform-aware config-root discovery (Linux: `$XDG_CONFIG_HOME/george`, otherwise `~/.config/george`; platform-equivalent behavior elsewhere), with injectable roots for tests:
- user-global `instructions.md`;
- user `personality.md`.

Support workspace-native `.george/instructions.md` plus root `AGENTS.md` and `BOOT.md`.

Treat `BOOT.md` primarily as routing guidance. Build a bounded deterministic catalog of safe workspace-relative text-document paths referenced by the routing source, but do not eagerly load every referenced body. Support explicit selected-route loading so a caller can include one routed document just in time. Reject traversal, symlink escape, unsafe/unsupported paths, and oversized sources explicitly.

Implement deterministic exact-duplicate suppression where safe without silently resolving conflicting sources.

Add token-oriented provider-facing size estimation and an injectable/configurable George context budget. A deterministic heuristic is acceptable when no exact tokenizer exists, but name/record it as an estimate. Raw byte bounds remain safety limits only. Critical required instruction sources must not be silently truncated and presented as complete; optional/routed sources may be omitted/deferred with recorded reason.

Do not add LLM summarization/compaction, skill support, session persistence, provider-specific tokenizers, browser/network access, or TUI changes in this prompt. Do not replace the current `AgentLoopApplicationService` context path yet.

Set `package.json` version to `0.3.1`.

Required proof:
- focused context tests cover source discovery/order/metadata, missing optional files, path/symlink containment, route catalog + selected-only body loading, duplicate handling, budget boundaries, oversized critical-source failure, optional omit/defer, and deterministic estimated-token diagnostics;
- personality remains a lowest-priority stylistic source and no context source can mutate executable policy;
- existing Phase-2 core/tool/provider/application/TUI tests remain Green;
- typecheck, broad applicable deterministic tests, and `git diff --check` pass;
- no `package-lock.json` exists.

If current source materially contradicts the approved Phase-3 contracts, stop with `Planning needed` rather than silently changing scope.
