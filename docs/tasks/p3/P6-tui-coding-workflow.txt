TASK: Phase 3 / P6 — Phase 3 TUI and CLI coding-workflow integration

MODEL / REASONING / USAGE
- Recommended configuration: `Terra High`.
- Browser required: no.

VERSIONING
- This prompt's assigned project version is `0.3.6`.

GOAL

Expose the qualified Phase-3 context/skills/session/completion capabilities through the Codex-style OpenTUI adapter without moving business logic into the UI.

Read Phase-3 authority/assessment/plan and inspect exact current TUI/main/config/application/session/skill APIs and test-renderer coverage.

Stop `GeorgeTui` from unconditionally creating a new session internally. The bootstrap/application path must create or open the session and pass it to the presentation adapter.

Add a minimal CLI resume option:
- `--resume <session-id>`.

Preserve the current optional positional workspace behavior. A resumed session must match the canonical selected workspace. Show enough session identity in the TUI status for a user to copy/reuse the ID. Do not build a session database/browser UI.

Add two presentation-only skill commands:
- `/skills` lists compact available skill name/description/source metadata without a provider call;
- `/skill <name> <task>` resolves exactly one explicit skill and submits `<task>` with that skill activated for the current turn.

Unknown/ambiguous skill names and missing task text must produce deterministic visible errors while leaving the composer usable. The TUI must call application/core skill APIs; it must not parse/read `SKILL.md` directly or own collision/permission policy. Do not build a generalized plugin command framework.

Render bounded context diagnostics from normalized evidence, including estimated token count labeled as estimated and useful active/omitted/deferred source counts/activated skill identity. Do not dump instruction bodies.

Render a compact post-turn coding summary from structured completion evidence: observed/direct/pre-existing changes, validation outcomes, and warnings/evidence gaps, while keeping the assistant narrative transcript separate.

Preserve existing OpenTUI behavior:
- streamed transcript;
- Enter/Shift+Enter behavior;
- draft preservation while a turn streams;
- scrollback;
- resize;
- approval allow/deny and process warning;
- Ctrl+C active cancellation and idle exit;
- clean renderer/terminal teardown.

Core/provider/application code must remain independent of OpenTUI.

Set `package.json` version to `0.3.6`.

Required proof:
- test renderer covers new session ID display, resumed transcript, `/skills`, successful `/skill`, unknown/ambiguous skill error, turn-scoped activation, context diagnostics, and completion summary;
- slash commands that do not invoke the model leave provider call count unchanged;
- resumed TUI never replays an interrupted side effect;
- all existing draft/approval/resize/cancel/cleanup tests remain Green;
- architecture-boundary tests remain Green;
- typecheck, broad deterministic tests, `git diff --check`, and no-`package-lock.json` checks pass.

If making these features usable requires OpenTUI to own session/context/skill policy, stop with `Planning needed`.
