TASK: Phase 3 / P7 — Integrated coding-workflow qualification

MODEL / REASONING / USAGE
- Recommended configuration: `Terra High`.
- Browser required: no.

VERSIONING
- This prompt's assigned project version is `0.3.7`.

GOAL

Qualify the exact integrated Phase-3 candidate and perform only bounded evidence-driven hardening. Do not expand the approved Coding Workflow scope.

Read `BOOT.md`, current product/architecture/workflow/stability contracts, the Phase-3 decision record, assessment/implementation plan, and exact P1-P6 source/tests.

Run and record the deterministic matrix:
- exact Petri phase-runner tests;
- core/config/context source/precedence/budget/routing tests;
- skill registry/discovery/collision/lazy-body/activation tests;
- durable session persistence/reconstruction/interruption/non-replay tests;
- provider tests;
- ToolRegistry/read-only/mutation/process/validation tests;
- coding-workflow integration tests;
- OpenTUI test-renderer skill/resume/context/completion plus existing approval/draft/resize/cancel behavior;
- architecture-boundary tests;
- TypeScript typecheck;
- broad `npm run check`;
- `git diff --check`;
- explicit no-`package-lock.json` check.

Build one deterministic end-to-end disposable Git repository fixture with:
- pre-existing dirty work;
- root routing/instruction material;
- one external portable `SKILL.md`;
- a scripted provider.

Drive a complete coding turn that explicitly activates the external skill, assembles context under the Phase-3 precedence/budget contract, performs at least one approved workspace mutation, runs at least one approved `run_validation`, reaches a final assistant response, emits structured change/validation/completion evidence, and persists the session.

Reopen that exact session and run a later unrelated turn. Prove completed transcript/history reconstructs, pre-existing dirty work remains intact, change attribution is truthful, and the prior skill body is not active unless explicitly activated again.

Also exercise an interrupted-side-effect fixture with incomplete write/process/approval/provider-continuation evidence and prove resume exposes interruption without replaying/restarting/reapproving/resubmitting it.

Audit persistent fixture files to ensure raw read-file bodies, raw write contents, unrestricted environment sentinels, and full skill bodies were not dumped by the durable evidence layer.

Live-model evidence:
- if the supported loopback LM Studio endpoint and an explicit supported Qwen model are available, run an opt-in bounded Phase-3 smoke using the exact candidate;
- exercise context assembly plus a harmless tool/validation cycle;
- record model/endpoint/runtime/timing/usage truth without dumping repository context/secrets;
- if unavailable, record `Evidence Gap`; do not download a substitute model.

Native-terminal evidence:
- if a genuine usable TTY exists, exercise native startup, session identity/resume, `/skills`, one explicit skill activation, approval interaction, cancellation, and terminal restoration where practical;
- otherwise record `Evidence Gap`.

Allow at most two substantial correction cycles for Phase-3 defects discovered by qualification. Every correction must add/retain a permanent executable regression detector for the defect class, rerun focused affected evidence first, then rerun the broad deterministic matrix. If a finding requires changing locked architecture or pulling Phase-4/5 scope forward, stop `Not Green / Planning needed`.

Create `docs/tasks/p3/P7-qualification-evidence.md` with exact source SHA/package/runtime and per-layer Green / Not Green / Evidence Gap results.

Set `package.json` version to `0.3.7`.

Required final state:
- required deterministic layers are Green or truthfully Not Green;
- the end-to-end fixture proves the Phase-3 success condition;
- non-replay, permission, process, Git-preservation, lazy-skill, and persistence-sanitization claims are backed by executable evidence;
- native/live gaps remain gaps when unavailable;
- typecheck and `git diff --check` pass when implementation is Green enough to hand off;
- no `package-lock.json` exists;
- no stray prompt-owned child processes remain.
