TASK: Phase 3 / P2 — Canonical agent-loop context integration and diagnostics

MODEL / REASONING / USAGE
- Recommended configuration: `Terra High`.
- Browser required: no.

VERSIONING
- This prompt's assigned project version is `0.3.2`.

GOAL

Replace the Phase-1/2 raw instruction concatenation in the canonical agent loop with the P1 provider-independent context assembler while preserving all safe-tool behavior.

Read the Phase-3 authority/assessment/plan and inspect exact P1 context code plus `src/application/one-turn.ts`, provider contracts, session/events, and deterministic agent-loop tests.

Integrate George-owned invariants, explicit current user input, prior transcript, optional user-global instructions/personality, workspace instructions, root repository guidance/routing metadata, tool definitions, and explicitly selected routed documents through the context layer.

Extend the presentation-independent run/service API with a bounded explicit selected-project-context input as needed. Routed document bodies must remain absent by default and appear only when selected or later read through normal tools.

Add normalized context diagnostics to application/session evidence without dumping full instruction bodies. Record stable source identities/classes, admission/omission/defer state, selected route identities, and estimated provider-facing size labeled as estimated. Preserve provider-reported actual usage separately when available.

Maintain truthful request-size diagnostics across multi-round tool continuations using a deterministic bounded approach.

Repository, workspace, personality, routed-document, or model text must not affect ToolRegistry permission classes, approval policy, filesystem boundaries, process policy, or network availability. Add explicit regression tests for that separation.

Remove/deprecate the old raw `instructionBytes` path if it would leave two competing context systems.

Preserve provider independence: LM Studio wire details remain in the adapter, and existing text/tool continuation behavior must not regress.

Do not add skills, durable sessions, coding summaries, validation orchestration, or TUI feature work yet.

Set `package.json` version to `0.3.2`.

Required proof:
- deterministic application tests prove precedence/order and provider-facing assembly;
- routed bodies are absent by default and selected only explicitly;
- estimated context diagnostics are emitted without full omitted-source bodies;
- personality/repository/routed text cannot bypass approvals or widen tool authority;
- multi-round tool/result continuation, invalid-call recovery, cancellation, loop ceilings, writes/process approvals, and TUI adapter tests remain Green;
- typecheck, broad deterministic tests, `git diff --check`, and no-`package-lock.json` checks pass.

If P1 architecture cannot safely integrate without changing the approved context/permission contracts, stop with `Planning needed`.
