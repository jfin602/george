TASK: Phase 3 / P5 — Changed-file evidence, validation workflow, and structured completion

MODEL / REASONING / USAGE
- Recommended configuration: `Terra High`.
- Browser required: no.

VERSIONING
- This prompt's assigned project version is `0.3.5`.

GOAL

Add structured coding-run evidence on top of the safe tool loop and durable session substrate without adding mutating Git or a privileged validation executor.

Read Phase-3 authority/assessment/plan and inspect exact mutation Git-snapshot helpers, process executor, ToolRegistry/approval flow, session store, application events, and tests.

At the beginning of a coding turn, capture a read-only Git working-tree baseline when available. Non-Git workspaces remain supported. At terminal completion, capture a final observable Git snapshot when available.

Track successful George-native `write_file` / `apply_patch` paths directly from tool results.

Build structured change evidence that distinguishes:
- pre-existing dirty paths;
- direct George-native mutation paths;
- newly observed dirty paths relative to baseline;
- final dirty paths;
- uncertainty for arbitrary child-process effects.

Do not claim causal attribution for process-modified files unless evidence establishes it. Preserve all pre-existing user changes and do not add reset/clean/stash/checkout or other mutating Git behavior.

Add a model-facing `run_validation` tool with the same bounded executable/argv/cwd/timeout contract and `process` permission class as `run_process`. It must delegate to the exact existing process execution implementation or a shared primitive extracted from it; do not fork spawn/environment/cleanup logic.

`run_validation` requires normal per-call process approval and preserves literal argv, `shell: false`, workspace cwd, sanitized environment, bounded output, timeout/cancellation, descendant cleanup, and truthful non-sandbox semantics.

Record ordered structured validation attempts including command identity/cwd, approval/denial/cancellation as applicable, terminal outcome, exit code/signal, and bounded runtime output evidence. Durable persistence must use P4 sanitization.

Add a structured application/core completion record/event containing at minimum final assistant response, context diagnostics summary/reference, change evidence, validation attempts, unresolved warnings/failures/evidence gaps, and terminal state. The TUI is not the source of truth.

A failed validation remains failed in completion evidence even if assistant prose says success.

Do not add TUI rendering or CLI resume/skill commands yet.

Set `package.json` version to `0.3.5`.

Required proof:
- clean baseline + direct write, pre-existing dirty preservation, direct mutation of already-dirty path, and final/new change classification;
- a process-created change is observed without being mislabeled as a direct George-native mutation;
- non-Git workspace degradation is truthful;
- `run_validation` is approval-required on every call and shares process environment/timeout/cancellation/cleanup behavior;
- failed/denied/cancelled validation evidence remains explicit;
- structured completion matches actual transcript/change/validation evidence and persists in sanitized form;
- no mutating Git command is introduced;
- all existing tool/provider/session tests remain Green;
- typecheck, broad deterministic tests, `git diff --check`, and no-`package-lock.json` checks pass.

Stop with `Planning needed` if validation cannot reuse the canonical process boundary.
