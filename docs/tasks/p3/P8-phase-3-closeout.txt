TASK: Phase 3 / P8 — Phase 3 evidence closeout

MODEL / REASONING / USAGE
- Recommended configuration: `Terra Medium`.
- Browser required: no.

VERSIONING
- This prompt's assigned project version is `0.3.8`.

GOAL

Perform an evidence-only Phase-3 closeout from the exact P7 candidate and its durable qualification evidence.

Read `BOOT.md`, current product/architecture/workflow/stability contracts, the Phase-3 decision record, assessment/implementation plan, exact P1-P7 source/test state, and `docs/tasks/p3/P7-qualification-evidence.md`.

Audit and report:
- provider-independent context source model, trust/precedence, routing, duplicate handling, budget behavior, and estimated-vs-actual usage truth;
- user-global instructions, personality, workspace instructions, root BOOT/AGENTS compatibility, and selected routed-document behavior;
- portable skill source discovery, stable identity/collision handling, metadata-only catalog, lazy body activation, turn scoping, and inability to raise executable authority;
- durable session state location/schema/workspace binding, reconstruction, sanitization, interruption evidence, and no-replay resume semantics;
- changed-file baseline/direct/new/final evidence and truthful uncertainty for arbitrary process changes;
- `run_validation` reuse of the canonical process executor/approval/non-sandbox boundary;
- structured validation and completion evidence;
- OpenTUI/CLI session-resume, skill activation, context diagnostics, completion rendering, and preservation of prior TUI behavior;
- Phase-2 permission, filesystem, process, Git, provider, and cancellation safeguards;
- deterministic end-to-end disposable-repository qualification;
- native terminal and live LM Studio/Qwen evidence;
- exact phase-runner and no-package-lock invariants;
- every Phase-4/5 feature remains deferred.

Do not repair implementation, add features, broaden tests merely to manufacture a pass, or advance Phase-4 behavior.

Create/update `docs/phase-3-closeout.md` with explicit Implementation Complete / Stability Qualified state and Green / Not Green / Evidence Gap truth. Narrowly update Phase-3 task status only when required to point at this formal evidence record.

Do **not** mark Phase 3 owner-closed or move `BOOT.md`/roadmap to Phase 4; owner closeout and the Phase-4 baseline transition are separate decisions.

Set `package.json` version to `0.3.8`.

Required proof:
- closeout claims trace to actual P7/current evidence;
- missing native/live evidence is never relabeled Green;
- no Phase-4/5 product scope is added;
- documentation `git diff --check` passes;
- no `package-lock.json` exists.
