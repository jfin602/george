TASK: Correction 9 / P2 — Fresh scoped Phase 9 live qualification

MODEL / REASONING / USAGE
- Recommended configuration: `Sol Medium`.
- Browser required: no.

VERSIONING
- Required unchanged project version: `0.9.10`.

GOAL

Use the corrected two-tier preflight to determine live eligibility, then spend the still-unspent Phase 9 live gates exactly once in order:

Gate A -> Gate B2 -> Gate C2.

This is qualification/evidence work only.

Read:
- `BOOT.md`;
- `AGENTS.md`;
- current Phase 9 qualification authority;
- `docs/planning/c9-qualification-preflight-scope/{decision-record,qualification-plan}.md`;
- `docs/tasks/c9-qualification-preflight-scope/{prompt-assessment,implementation-plan,P1-preflight-evidence}.md`;
- committed P1 broad comparison JSON;
- `docs/tasks/c9-mutation-precondition-convergence/{P2-live-evidence,closeout}.md`;
- exact current code/tests;
- frozen Gate A fixture/helper;
- unchanged v2 instruments/metadata/acceptance;
- live-work artifact writer.

Do not modify production code, qualification policy, or live instruments in P2.

PRE-GATE — HARD CURRENT CANDIDATE

Require Green:

- qualification-preflight classifier tests;
- mutation-precondition exact failure-class regressions;
- expanded inherited c9 affected floor;
- Phase 9 integration;
- frozen Gate A deterministic replay;
- v1/v2 fixture/task/metadata/digest/acceptance immutability;
- `npm run typecheck`;
- `npm run test:runner`;
- `git diff --check`;
- package exactly `0.9.10`;
- no root `package-lock.json`.

Any hard Not Green result stops live work.

BROAD CURRENT CHARACTERIZATION

Run current `npm test` under supported Node 26.

Preserve its aggregate state exactly.

Use the P1 controlled baseline summary plus current candidate summary with the qualification classifier.

Live eligibility requires:
- hard preflight Green;
- broad regression delta Green.

It does NOT require aggregate broad-suite Green.

If current broad failures are new/worsened or comparison becomes Evidence Gap:
- stop;
- record Gate A/B2/C2 Not Run;
- do not repair/rerun.

RUNTIME CONTROL

Use exactly:

`qwen3-coder-30b-a3b-instruct@q4_k_m`

Use supported Node 26.

Before live work:
- print `MANUAL CHECK: LM Studio GPU Offload must show 26`;
- capture exact REST-visible model/runtime controls;
- do not retune context/runtime/convergence/retry/correction/stage/run-budget settings.

UI-only GPU Offload 26 remains Evidence Gap if not independently observed.

GATE A — FRESH

Only after scoped preflight eligibility is Green.

Run exactly one official attempt using unchanged:
- frozen Gate A task/fixture;
- fair Phase 9 qualification approval;
- validation;
- hidden acceptance;
- production structured service.

Record exact edit, V1, TaskState, acceptance, calls/rounds/retries/tokens/context/budgets/timing/intervention and every tool/approval terminal state.

Green requires:
- exact 60-byte result;
- V1 Green;
- completed/verified TaskState;
- hidden acceptance Green;
- zero intervention;
- no exhaustion;
- <=10 model-requested tool calls;
- <=10 logical provider rounds.

If Not Green:
- STOP;
- do not run B2/C2;
- no repair/rerun.

GATE B2 — GREENFIELD V2

Only after Gate A Green.

Run exactly one fresh official `greenfield-express-v2` attempt using unchanged:
- version/digest;
- acceptance version 1;
- exact deliberate `npm install express@5.1.0` prerequisite;
- production structured stack service.

Record:
- dependency authorization/result;
- StackState progression;
- INSPECT;
- every requested/started/terminal tool call;
- SHA read/retry path;
- missing-parent/create_directory path if exercised;
- mutations;
- George-owned validations;
- hidden acceptance;
- calls/rounds/tokens/context/budgets/timing/intervention.

Green requires all P1/P2/P3 completed, validations Green, StackState completed, hidden acceptance Green, zero intervention, no exhaustion.

If Not Green:
- STOP;
- leave C2 unspent;
- no repair/rerun.

GATE C2 — EXISTING APP V2

Only after B2 Green.

Run exactly one official `existing-express-feature-v2` attempt using unchanged fixture/task/acceptance/prerequisite.

Require:
- focused validation Green;
- broad validation Green;
- completed/verified TaskState;
- baseline behavior preserved;
- hidden acceptance Green;
- zero intervention;
- no exhaustion.

COMMITTED EVIDENCE

Create:

`docs/tasks/c9-qualification-preflight-scope/P2-live-evidence.md`

Update/create bounded:
- `evidence/current-broad.json`;
- `evidence/current-delta.json`;
- `evidence/runtime.json`;
- `evidence/gate-a/{attempt,trace}.json` if reached;
- `evidence/gate-b2/{attempt,trace}.json` if reached;
- `evidence/gate-c2/{attempt,trace}.json` if reached.

Use the repository-defined sanitized live-work schema for attempt/trace files.

Record SHA-256 for each committed JSON.

Never commit raw provider streams, file/patch bodies, secrets/environment dumps, dependency workspaces, node_modules, or unbounded logs.

Preserve historically:
- old Gate A Green;
- v1 Not Green;
- first B2 Not Green;
- mutation-precondition P2 preflight-stopped result.

Do not:
- modify production code;
- edit instruments/acceptance;
- retune runtime/limits;
- rerun a failed official gate;
- relabel aggregate broad state;
- owner-close Phase 9;
- open Phase 10.
