TASK: Correction 9 / P3 — Gated Qwen work-unit convergence qualification

MODEL / REASONING / USAGE
- Recommended configuration: `Sol Medium`.
- Browser required: no.

VERSIONING
- Required unchanged project version: `0.9.10`.

GOAL

Qualify the corrected safe edit-enablement path against pinned live Qwen using reliable repository-owned telemetry. Spend the larger frozen Phase 9 workloads only after the smallest exact edit-plus-validation gate is Green.

Read:
- `BOOT.md`;
- `AGENTS.md`;
- current Phase 9 authority;
- `docs/tasks/c9-structured-task-convergence/{P5-live-evidence,closeout}.md`;
- `docs/tasks/c9-qwen-workunit-convergence/{prompt-assessment,implementation-plan}.md`;
- exact P1/P2 candidate source/tests;
- preserved Phase 8 / first Phase 9 / first c9 live evidence;
- repository-owned live-work runner/trace;
- frozen P4, greenfield, and existing-app instruments.

Do not change production code in P3.

RUNTIME CONTROL

Use exactly:
`qwen3-coder-30b-a3b-instruct@q4_k_m`

Use supported Node 26.

Before live work:
- print `MANUAL CHECK: LM Studio GPU Offload must show 26`;
- capture REST-visible model ID, context length, eval batch, physical batch, parallel, Flash Attention, GPU KV cache, and expert count;
- do not silently alter runtime/context settings;
- UI-only GPU Offload 26 remaining unconfirmed limits controlled latency claims but does not invalidate functional evidence.

If pinned LM Studio/Qwen is unavailable, record Evidence Gap and do not substitute another model.

DETERMINISTIC PRE-GATE

Require Green:
- P1 hash/precondition tests;
- dynamic read -> hash -> mutation deterministic replay;
- P2 live-trace tests;
- frozen deterministic Phase 8 structured replay;
- Phase 9 integrated qualification;
- affected c9 convergence/stack regression floor;
- `npm run typecheck`;
- `npm run test:runner`;
- `git diff --check`.

GATE A — EXACT FROZEN EDIT + VALIDATION

Run exactly one new official live attempt of the frozen Phase 8 structured counterpart.

Do not modify the frozen task.

The repository-owned trace must be persisted before optional report rendering.

Gate A is Green only if ALL are true:
- implementation receives an observed `read_file.sha256` for the target file;
- the exact required edit is made;
- any overwrite/patch mutation uses the appropriate observed current hash;
- George-owned V1 passes;
- final TaskState is completed with covered requirements verified;
- zero human coding intervention;
- no correction/task/stage budget exhaustion;
- <=10 model-requested tool calls;
- <=10 logical provider rounds.

Record:
- exact ordered tool calls;
- requested mutation path and expectedSha256, bounded safely;
- mutation terminal result;
- duplicate/no-progress events;
- provider attempts/rounds/retries;
- input/output tokens;
- selected/attempted context profiles and estimated input;
- task/stage/run budgets;
- validation/correction history;
- elapsed/workflow timing;
- final TaskState;
- exact artifact paths.

Compare truthfully with:
- preserved Phase 8 Not Green: 10 calls, 7 rounds, 20,765 / 406 tokens, 45.826 s;
- first Phase 9 Not Green: 25 calls, 20 rounds, 63,253 / 1,801 tokens, 344.745 s;
- first c9 Gate A Not Green: exact edit absent, telemetry Evidence Gap.

If Gate A is Not Green:
- STOP;
- do not run greenfield;
- do not run existing-app;
- do not rerun until pass;
- do not repair implementation in P3.

GATE B — GREENFIELD PRODUCTION STACK

Only after Gate A Green.

Run exact `greenfield-express-v1` through the already-qualified production structured stack executor.

Use the existing explicit dependency-install authorization contract. If the required network/dependency authorization is not available, classify Gate B as Evidence Gap; do not silently enable network.

Green requires:
- P1/P2/P3 all completed in strict production stack order;
- all declared validations Green;
- final StackState completed;
- hidden acceptance Green;
- zero human coding intervention;
- no task/stage budget exhaustion;
- no unbounded duplicate/no-progress loop.

Production fail-stop semantics remain authoritative: a failed earlier task means later tasks do not run.

If Gate B is Not Green, STOP and do not run Gate C.

GATE C — EXISTING APP

Only after Gate B Green.

Run exactly one official `existing-express-feature-v1` attempt.

Green requires:
- focused validation Green;
- broad validation Green;
- completed TaskState;
- baseline tests preserved;
- hidden acceptance Green;
- zero human coding intervention;
- no task/stage budget exhaustion.

EVIDENCE

Create:
`docs/tasks/c9-qwen-workunit-convergence/P3-live-evidence.md`

Include:
- exact candidate SHA/package;
- runtime provenance;
- deterministic pre-gate;
- Gate A/B/C outcomes only where reached;
- full bounded repository-owned trace;
- mutation-precondition evidence;
- raw call/round/token/time/budget/correction dimensions;
- hidden acceptance;
- Green / Not Green / Evidence Gap matrix;
- artifact paths.

Do not:
- repair production code;
- alter convergence limits;
- retune context/runtime;
- rerun a failed official workload;
- owner-close Phase 9;
- open Phase 10.

Package remains exactly `0.9.10`; no root lockfile; `git diff --check`.
