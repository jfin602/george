GEORGE TASK FORMAT: 1

STACK: p9
TASK: Phase 9 / P3 — Structured orchestration and bounded task slices
KIND: implementation

VERSIONING

The assigned project version is `0.9.3`.

- Recommended configuration: `Terra High`.
- Browser required: no.

GOAL

Implement the structured-task application/orchestration path over the existing AgentLoop/CodingWorkflow services so George owns task sequencing, inspection, bounded provider slices, and validation truth while ordinary chat remains unchanged.

READ FIRST

- REQUIRED: BOOT.md
- REQUIRED: AGENTS.md
- REQUIRED: docs/planning/p9-structured-task-execution/decision-record.md
- REQUIRED: docs/planning/p9-structured-task-execution/task-format-v1.md
- REQUIRED: docs/planning/p9-structured-task-execution/qualification-plan.md
- REQUIRED: docs/tasks/p9/implementation-plan.md
- REQUIRED: docs/phase-8-owner-closeout.md
- REQUIRED: docs/stability-contract.md

INSPECT

- P1 parser and P2 TaskState APIs
- AgentLoopApplicationService submission/context/tool selection
- CodingWorkflowApplicationService validation behavior
- context routed-document/skill interfaces
- progress/work and durable event contracts
- benchmark agentic edit fixture for later P4 equivalence

REQUIREMENTS

- R1: Add a provider-independent StructuredTaskApplicationService above the existing agent/coding workflow rather than putting task semantics in OpenTUI or the LM Studio provider.
- R2: Detect/parse the Task Prompt v1 marker before provider/tool execution; invalid structured tasks fail closed, while unmarked ordinary chat keeps the existing path unchanged.
- R3: Resolve READ FIRST through canonical workspace/context routing: REQUIRED failure blocks before provider execution and OPTIONAL failure is visible non-blocking evidence.
- R4: When INSPECT exists, run a bounded read-only inspection preflight and require actual read/list/search/Git evidence before mutating work can advance; narration alone is not inspection evidence.
- R5: Execute ready work units in dependency order and update TaskState only from application/provider/tool/validation lifecycle evidence.
- R6: Build a bounded provider task slice from canonical TaskState containing the current work unit, covered requirements, applicable invariants, active validation/stops, relevant routed/inspection/recovery evidence, and bounded current failure evidence; omit completed unrelated/stale ledger material by default.
- R7: Do not introduce a guessed context-token threshold; pass the derived slice through inherited Phase 3/8 context assembly/profile policy and preserve P7 continuation-pressure safety.
- R8: Use deterministic stage-appropriate provider tool exposure: inspection is local read-only; structured coding stages exclude unrelated external/browser/remote capabilities by default; validation is George-owned.
- R9: Execute literal VALIDATION entries through existing shell-free process/approval/budget evidence and record actual result state; model assertion cannot satisfy validation.
- R10: Support a bounded DISCOVER validation proposal path tied to Scope where the model proposes explicit executable/argv and George validates/executes it through the same process policy.
- R11: Emit bounded task lifecycle/projection events suitable for persistence/TUI without raw task/provider/tool-result bodies.
- R12: Set package version to 0.9.3 and add permanent structured-orchestration/task-slice regression coverage.

INVARIANTS

- I1: Full canonical TaskState is never injected solely because it exists.
- I2: Structured orchestration cannot weaken ToolRegistry validation, approvals, budgets, context precedence, retries, compaction, recovery, hooks, or session authority.
- I3: Ordinary unmarked chat behavior remains backward compatible.
- I4: External/network tools remain independent capabilities and are not implicitly granted by structured work.
- I5: No package-lock.json is created.

WORKFLOW

W1 — Add structured service boundary and submission routing
Covers: R1, R2, R5, R11
Depends on: none

Use the P1/P2 contracts and existing application services.

W2 — Implement READ FIRST and INSPECT preflight
Covers: R3, R4, R8
Depends on: W1

Make inspection evidence application-owned and keep mutation/process authority out of the preflight.

W3 — Implement bounded task-slice projection
Covers: R6, R7, R8
Depends on: W1, W2

Add deterministic tests proving unrelated completed ledger content does not appear in provider requests.

W4 — Integrate validation and one-pass work-unit progression
Covers: R5, R9, R10
Depends on: W3

Use existing process/approval/budget paths; no shell.

W5 — Regression and version boundary
Covers: R12
Depends on: W1, W2, W3, W4

VALIDATION

V1 — Structured application coverage
Covers: R1, R2, R3, R4, R5, R6, R7, R8, R9, R10, R11, R12
Run: node --test test/unit/tasks/*.test.ts test/unit/application/structured-task.test.ts

V2 — Agent/context regressions
Covers: R6, R7, R8
Run: node --test test/unit/application/one-turn.test.ts test/unit/context/index.test.ts test/unit/context/profile-selector.test.ts

V3 — Coding workflow regression
Covers: R9, R10
Run: node --test test/unit/application/coding-workflow.test.ts

V4 — TypeScript
Covers: R1, R6, R11
Run: npm run typecheck

V5 — Runner regression
Covers: R12
Run: npm run test:runner

V6 — Diff hygiene
Covers: R12
Run: git diff --check

STOP CONDITIONS

- S1: Return Planning needed if bounded task slicing requires changing the approved Task Prompt grammar or Phase 3/8 context authority.
- S2: Do not implement correction-loop, TUI, autonomous filesystem, or sandbox behavior in P3.
- S3: Do not silently mark INSPECT/validation complete from model narration.
- S4: Do not claim completion while focused, inherited, typecheck, runner, or diff validation is failing.

DELIVERABLES

- D1: StructuredTaskApplicationService and task submission routing.
- D2: READ FIRST/INSPECT/work-unit/task-slice behavior.
- D3: One-pass literal/DISCOVER validation integration.
- D4: package.json at 0.9.3.

NON-GOALS

- Bounded correction/revalidation loops beyond one-pass evidence.
- Task stack sequencing/resume hardening.
- TUI Task page.
- Workspace Autonomous mode.
- Performance/context retuning.

EVIDENCE

- Tests must demonstrate canonical TaskState content omitted from provider slices when not applicable.
- Verify no root package-lock.json.
