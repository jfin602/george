TASK: Correction 9 / P3 — Inspection stage completion correction closeout

MODEL / REASONING / USAGE
- Recommended configuration: `Sol Medium`.
- Browser required: no.

VERSIONING
- Required unchanged project version: `0.9.10`.

GOAL

Perform an evidence-only closeout of `c9-inspection-stage-completion` from the exact P2 candidate.

Read:
- `BOOT.md`;
- current architecture/workflow/stability/roadmap contracts;
- Phase 9 authority and closeout;
- prior c9 correction closeouts;
- `docs/tasks/c9-inspection-stage-completion/{prompt-assessment,implementation-plan,P2-live-evidence}.md`;
- exact current source/tests.

Audit separately:

INSPECT STAGE COMPLETION IMPLEMENTATION
- generic application-owned successful-tool-round completion mode;
- absent/default behavior unchanged;
- full provider tool round executes before completion;
- provisional assistant text remains non-canonical;
- no provider continuation after successful bounded round;
- failure/denial-only round does not falsely complete;
- stage limits remain fallback guards;
- structured INSPECT is the only Phase 9 caller using this completion mode.

LIVE GATE A
- first-round required inspection tool choice;
- first-round read-only calls/results;
- proof of application-owned INSPECT completion;
- implementation start;
- read SHA/mutation precondition;
- exact edit;
- George-owned V1;
- final TaskState;
- model tool calls/provider rounds/tokens/context/budgets/timing;
- intervention/exhaustion truth.

LARGER LIVE GATES
- Gate B greenfield only if Gate A allowed it;
- Gate C existing-app only if Gate B allowed it;
- hidden acceptance and stack/task truth;
- every stop reason.

INHERITED QUALIFIED BOUNDARIES
- production task-stack execution;
- bounded evidence handoff/task-wide budget/direct validation/convergence guards;
- read SHA/mutation precondition;
- mandatory first-round INSPECT tool execution;
- exception-safe live trace/artifact retention;
- Bubblewrap containment;
- unchanged context/runtime policy;
- native TUI Evidence Gap remains explicit unless independently resolved.

Create:

`docs/tasks/c9-inspection-stage-completion/closeout.md`

Report separately:
- Implementation Complete: yes/no;
- INSPECT Stage Completion Qualified: yes/no;
- Gate A Qwen Work-Unit Convergence Qualified: yes/no;
- Gate B Greenfield Qualified / Not Run;
- Gate C Existing-App Qualified / Not Run;
- Overall c9-inspection-stage-completion Qualified: yes/no.

QUALIFICATION RULES

INSPECT Stage Completion Qualified requires:
- deterministic early-completion regression floor Green;
- live Gate A proves at least one successful inspection tool round advances into implementation without a second INSPECT provider round or stage exhaustion.

Qwen Work-Unit Convergence Qualified requires Gate A Green under the locked criteria:
- exact edit;
- V1 pass;
- completed/verified TaskState;
- zero intervention;
- no exhaustion;
- <=10 model tool calls;
- <=10 provider rounds.

Overall correction qualification requires Gate A Green.

Do not waive Gate A.

Do not:
- repair implementation;
- rerun live workloads;
- rewrite historical evidence;
- owner-close Phase 9;
- open Phase 10;
- change BOOT/roadmap;
- change package version.

Required proof:
- focused P1 regressions;
- frozen deterministic replay;
- Phase 9 integration;
- inherited c9 affected floor;
- `npm run typecheck`;
- `npm run test:runner`;
- appropriate broad characterization with inherited OpenTUI gaps visible;
- `git diff --check`;
- package exactly `0.9.10`;
- no root `package-lock.json`.

Owner acceptance and Phase 9 owner closeout remain separate actions.
