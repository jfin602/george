# Phase 8 closeout — Context Throughput Optimization

Status: **IMPLEMENTATION COMPLETE; STABILITY QUALIFIED DETERMINISTICALLY; NOT STABILITY GREEN**

Date: 2026-09-24  
Assigned package: `0.8.8`  
Exact final candidate: post-P7 commit `90e94e54ecef61e484fc0a9bb2a609fb4cff4d91` (`0.8.7`), with this closeout-only working tree changing `package.json` to `0.8.8` and adding this record. This is not P6's pre-P7 candidate `0c2e812fdc18f9255240c0a1c5b56b7fe505cdbd`.

## Scope and evidence basis

This is an evidence-only closeout. It reconciles the P6 integrated baseline in `docs/tasks/p8/P6-qualification-evidence.md` with P7's final continuation-pressure correction and evidence in `docs/tasks/p8/P7-continuation-pressure-evidence.md`, against the current candidate source and tests. P3's adaptive qualification record, P4's stable-prefix record, and P5's provider-reuse record remain part of the result; rejected experiments were not revived.

| Layer | State | Current-source and evidence truth |
| --- | --- | --- |
| Adaptive/fixed configuration and profile values | Green | `src/core/config.ts` defines explicit adaptive/fixed mode and ordinary (`4,096–6,144`, soft `7,168`, input ceiling `8,192`), medium (`8,192–12,288`, `14,336`, `16,384`), and large (`12,000–18,000`, `20,000`, `24,576`) profiles. Each targets physical `32,768`, reserves `8,192`, and has always-on target `2,560`; large remains the compatible default. P6 focused deterministic qualification passed. |
| Selection, promotion, omission, and defer | Green | `src/context/profile-selector.ts` probes monotonically ordinary -> medium -> large, promotes required-source failure, soft pressure, selected project instructions, routed documents, and activated skills, while preserving existing whole-source optional omission/defer behavior. P6 records fixed overrides, deterministic selection, selected-material promotion, and optional defer as Green. |
| Per-turn immutability and selection neutrality | Green | `src/application/one-turn.ts` selects before the first provider request and builds one canonical selected assembly; discarded probes are non-canonical. P6 and `profile-selector`/`one-turn` tests prove no probe provider, tool, hook, session, permission, or canonical source-lifecycle side effect, and exactly one canonical assembly. |
| Precedence, provenance, and correctness-critical context | Green | The existing assembler remains the sole assembly authority. P6's focused 89/89 coverage records stable identity/order/precedence, required-source retention, and source dispositions. P7's ordinary guard proves current intent, George invariants, selected routed guidance, activated skill guidance, and authoritative unresolved recovery state remain in every safe continuation; the medium guard proves complete frozen requests remain intact through substantial results. |
| Ordinary/medium compaction and Phase-5 preservation | Green | Ordinary and medium promote before semantic compaction and make zero compactor calls. Large retains the Phase-5 soft/hard pressure, checkpoint, cancellation, recovery, and canonical-history path; P6 records its focused recovery/compaction coverage Green. Ordinary/medium are not semantically compacted merely to remain small. |
| Frozen continuation pressure (P7 final gate) | Green, deterministic | P7 corrected P6's unbounded accumulated tool-result gap. `src/application/one-turn.ts` estimates completed serialized tool results before a continuation and fails with a bounded `budget` error above the frozen profile ceiling; it also fails if reported provider input exceeds that ceiling. It does not drop, summarize, reroute, resize, compact, or make an unsafe extra model call. P7's 91/91 focused run proves an ordinary turn stops before its fourth unsafe request after three substantial file results, and a medium turn completes three safe rounds at reported `16,000` input tokens. |
| Selected-profile diagnostics | Green | Canonical `context.assembled` diagnostics retain bounded mode, selected profile/value, attempted IDs, promotion reasons, token totals, headroom, pressure, categories, active source IDs, and disposition evidence. P6 records deterministic diagnostic coverage Green. |
| P4 stable-prefix experiment | Green decision: rejected | P4 tested only canonical JSON key ordering, found no attributable provider-input, prefix-cache, or latency benefit, and reverted it. The retained state keeps byte-stable logical provider-facing context without claiming cache reuse. P4's final focused 71/71 and typecheck/runner checks passed; its live control remained an Evidence Gap for UI-only GPU Offload 26. |
| P5 provider-native reuse and fallback | Green decision: rejected | P5 omitted unchanged continuation instructions/tools and observed lower reported input but incorrect structured-tool final answers. The compact body and fallback experiment were reverted. Current `src/provider/lm-studio.ts` sends complete instructions/tools on continuations plus transport-only `previous_response_id`; P5 fixtures prove rejected continuation, cancellation, and provider errors do not blindly replay. George normalized session/context history remains canonical. |
| Benchmark harness and controlled provenance | Green deterministic / Evidence Gap live control | P6 records the deterministic harness/parser, including requested `24,576` acceptance and `24,577` rejection, as Green. Its context artifact is `artifacts/benchmarks/2026-09-24T22-20-15-033Z-186148`; quick artifact is `artifacts/benchmarks/2026-09-24T22-21-14-855Z-187523`. REST proved 32k context, eval `2048`, physical batch `512`, parallel `1`, Flash Attention/KV offload enabled, and 8 experts, but UI-only GPU Offload `26` was not independently observed. Therefore those live measurements are characterization, not an accepted comparable performance claim. |
| Provider input, latency, and provider-call effects | Not Green for live qualification | P6 context characterization was 14/15 after one pre-response provider failure at 8,192; P6 quick was 11/12 after a duplicate structured-tool call. P3's earlier complete ladder was likewise 14/15. P5's lower continuation input was rejected for correctness. P7 made no live latency/cache/capacity claim. No uncontrolled or failed live evidence is relabeled Green. |
| Deterministic regression floor | Green | P7 records typecheck pass, focused benchmark/context/compaction/application/recovery/provider/integration tests 91/91, and phase runner 90/90. The P7 guards are permanent regression coverage for frozen ordinary/medium continuation growth. |
| Broad/native TUI validation | Not Green | P7 direct broad run was 287/314; all 27 inherited OpenTUI native-FFI initialization failures remain on Node `v24.21.0`. `npm test` cannot start because that runtime rejects required `--experimental-ffi`. The separate pre-existing TUI streaming assertions remain Not Green, not Green by omission. |
| Native interactive TUI and live continuation pressure | Evidence Gap | No genuine interactive TTY validation and no live LM Studio P7 continuation-pressure run were available. P7 makes no live provider-capacity, latency, or cache assertion. |

## Stability verdict

**Implementation Complete:** adaptive/fixed policy, deterministic selection/promotion diagnostics, P4/P5 rejection/fallback state, and the P7 frozen-envelope safety correction are present in the exact post-P7 candidate.

**Stability Qualified:** deterministic evidence qualifies the policy and final continuation-pressure behavior. In particular, initially ordinary or medium turns may grow through substantial tool/file results without silently losing current, routed, project, skill, or recovery context. The selected profile is immutable; when its frozen provider-input envelope becomes unsafe, George stops with an explicit bounded budget failure rather than silently discard critical context, semantically compact ordinary/medium merely to stay small, resize mid-turn, or submit an extra unsafe continuation.

**Not Stability Green:** live context/quick characterization failures, missing independently observed GPU Offload 26 control, unavailable native TUI runtime, and missing live P7 continuation-pressure evidence prevent an all-green stability claim. These states include the unrelated pre-existing TUI failures and do not attribute them to Phase 8.

## Integrity

- P6 and P7 both preserve Phase-5 compaction/recovery, replay-safe continuation recovery, and canonical George authority; provider-native response/cache state is optional transport optimization only.
- P4 and P5 are explicitly rejected and retained as evidence; no further optimization was attempted.
- `package-lock.json` is absent.
- This document does not owner-close Phase 8, alter BOOT/roadmap, or advance Phase 9.
